<?xml version="1.0" encoding="utf-8"?>

<!--These has are the elements for a single line. This gets looped by the ArrayAdapter so don't worry-->

<!--    Instead of wrapping this up as we did in activity_main.xml, we simply put one line of code in activity_words.xml which does the same job! - android:drawSelectorOnTop="true"-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="@dimen/list_item_height"
    android:background="@color/tan_background">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="@dimen/list_item_height"
        android:layout_height="@dimen/list_item_height"/>

    <LinearLayout
        android:id="@+id/textContainer"
        android:layout_width="match_parent"
        android:layout_height="@dimen/list_item_height"
        android:orientation="vertical"
        android:paddingLeft="16dp"
        android:layout_toRightOf="@+id/imageView"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/miwok_textView"
            style="@style/ListStyle"
            android:textStyle="bold"
            tools:text="lutti"/>

        <TextView
            android:id="@+id/default_textView"
            style="@style/ListStyle"
            tools:text="one" />
    </LinearLayout>

    <ImageView
        android:id="@+id/playIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_centerVertical="true"
        android:layout_marginRight="16dp"
        android:layout_alignParentRight="true"
        android:src="@drawable/arrow_white"/>



</RelativeLayout>
